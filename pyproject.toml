[project]
name = "tszpaint"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jax>=0.8.0",
]

[dependency-groups]
dev = [
    "ipython>=9.7.0",
]
